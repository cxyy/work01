项目遗留的一些不合理场景

1、由于为了容易扩展表格需求，没有对ele表格进行封装，在项目收尾时，要求加一条查询不到数据给出 '没有查询到数据' 的提示，
为了方便再次更改需求，使用了vue的mixin替代方案，每个页面都导入了mixin去监听页面实例中data选项下的tableData，所以页面这个变量不能变，
如果要变，请去除该页面的混合。   如果更改文案或逻辑，直接更改mixin。
当然可以酌情项目复杂和扩展度去封装QTable组件
